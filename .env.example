# Database Configuration
POSTGRES_DB=signalsboard
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# API Configuration
ASPNETCORE_ENVIRONMENT=Development
DB_CONNECTION_STRING=Host=postgres;Database=signalsboard;Username=postgres;Password=your_secure_password_here

# Instructions:
# 1. Copy this file to .env
# 2. Replace 'your_secure_password_here' with actual secure passwords
# 3. Never commit .env to source control
# 4. For production, use Docker Secrets, Azure Key Vault, or similar