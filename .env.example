# Hospital Dashboard Environment Configuration Template
# Copy this file to .env.local (development) or .env.production (CI/CD)
# DO NOT commit .env.local or .env.production - they contain sensitive values

# ============================================
# ENVIRONMENT SELECTION (Single Source of Truth)
# ============================================
# development → DEBUG build, verbose logging, development tools
# production → RELEASE build, minimal logging, optimized
SIGNALSBOARD_ENVIRONMENT=development

# ============================================
# DATABASE CONFIGURATION
# ============================================
POSTGRES_DB=signalsboard
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# Connection string for .NET application
DB_CONNECTION_STRING=Host=postgres;Port=5432;Database=signalsboard;Username=postgres;Password=your_secure_password_here

# ============================================
# DOCKER BUILD ARGUMENTS (Derived by build.sh)
# ============================================
# build.sh automatically derives these from SIGNALSBOARD_ENVIRONMENT:
# development → BUILD_CONFIGURATION=Debug, ASPNETCORE_ENVIRONMENT=Development
# production → BUILD_CONFIGURATION=Release, ASPNETCORE_ENVIRONMENT=Production
# (Do NOT set manually - let build.sh derive them)

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Local Development:
#    cp .env.example .env.local
#    Update POSTGRES_PASSWORD with your local value
#    Run: ./build.sh development
#
# 2. Production Deployment (CI/CD):
#    Set environment variables in CI/CD secrets manager
#    CI/CD pipeline will load these values
#    Example: ./build.sh production